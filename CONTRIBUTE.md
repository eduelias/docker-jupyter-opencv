# Those are instructions to contribute to this repository.

If you want more libraries in the container, just add a new line, after the `RUN apt ....` line, as:
```Dockerfile
RUN apt install your-library-here
````
